#!/usr/bin/env bash
set -euo pipefail

remote_user="${USER:-$(id -un)}"
export PYTHONPATH="/users/${remote_user}/src/useful-notebooks:${PYTHONPATH:-}"
python_bin="${PYTHON:-/user-environment/env/default/bin/python}"

exec "${python_bin}" -m useful_notebooks_cp2k_unfolding.unfold_mpi_cli "$@"
